# iot-predictive-maintenance
A machine learning system that predicts industrial equipment failures before they occur by fusing IoT sensor telemetry (vibration, temperature, current) with external environmental data (weather, factory load). Built with LightGBM, SMOTE, and SHAP explainability on the AI4I dataset — deployed as an interactive maintenance dashboard.
